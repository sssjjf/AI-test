<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ExpressDao">
    <insert id="insertExpress" >
        INSERT INTO express(date,name,number,code,address,company,belongTo) VALUES(#{date},#{name},#{number},#{code},#{address},#{company},#{belongTo});
    </insert>

    <select id="findAll" resultType="com.example.demo.bean.Express">
        SELECT * FROM express
    </select>

    <delete id="deleteExpressByCode">
        DELETE FROM express WHERE code=#{code}
    </delete>
</mapper>