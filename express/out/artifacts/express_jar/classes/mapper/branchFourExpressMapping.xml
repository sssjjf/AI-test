<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.BranchFourExpressDao">
    <insert id="insertExpress" >
        INSERT INTO express_c4 (date,name,number,code,address,company) VALUES(#{date},#{name},#{number},#{code},#{address},#{company});
    </insert>

    <select id="findAll" resultType="com.example.demo.bean.BranchExpress">
        SELECT * FROM  express_c4
    </select>

    <delete id="deleteExpressByCode">
        DELETE FROM express_c4 WHERE code=#{code}
    </delete>
</mapper>